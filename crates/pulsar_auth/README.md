# Pulsar Auth

Pulsar auth provides the utilities to the rest of the engine for auth state management. Auth takes places via GitHub OAuth and the session is used for multiuser editing (User trust verification, name and profile picture display, etc), automatic git authentication (Coming soon), automatic copilot authentication (Coming soon), and probably more in the future
