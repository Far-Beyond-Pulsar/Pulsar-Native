[package]
name = "pulsar_diagnostics"
edition = "2021"
version = "0.1.0"
description = "Shared diagnostic types for the Pulsar engine"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
thiserror = "1"

[lints]
workspace = true
