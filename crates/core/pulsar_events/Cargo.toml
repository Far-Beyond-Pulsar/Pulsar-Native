[package]
name = "pulsar_events"
version = "0.1.0"
edition = "2024"
description = "Engine-wide event plumbing for Pulsar: asset-update notifications that editors, plugins and the game runtime publish and subscribe to by asset kind."

[dependencies]
ui_types_common = { workspace = true }
serde           = { workspace = true, features = ["derive"] }
tracing         = { workspace = true }

[lints]
workspace = true
