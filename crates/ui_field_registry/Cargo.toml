[package]
name = "ui_field_registry"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true, features = ["derive"] }
ui_gen_macros= { workspace = true, optional = true }

[features]
default = []
derive = ["ui_gen_macros"]
