//! Object CRUD, hierarchy queries, property/selection setters, ordering,
//! duplication, and the folder helper.
//!
//! Split by responsibility across [`crud`], [`queries`], and [`setters`].

mod components;
mod crud;
mod queries;
mod setters;
