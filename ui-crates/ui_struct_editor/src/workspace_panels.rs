/// Workspace panel wrappers for dock system integration
///
/// These wrappers allow struct editor components to be embedded
/// in the dock-based workspace panel system.

use gpui::*;
use ui::dock::{Panel, PanelEvent, PanelView};

// Future workspace panel implementations will go here
// For now, the main StructEditor acts as its own panel
