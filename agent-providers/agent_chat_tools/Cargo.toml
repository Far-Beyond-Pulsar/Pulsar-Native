[package]
name = "agent_chat_tools"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
anyhow = "1"
serde_json = "1"
plugin_manager = { path = "../../crates/plugin_manager" }
reqwest = { version = "0.12", features = ["blocking", "json", "rustls-tls"] }
scraper = "0.20"
urlencoding = "2"
