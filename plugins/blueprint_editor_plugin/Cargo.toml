[package]
name = "blueprint_editor_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
# Plugin API
plugin_editor_api = { path = "../../crates/plugin_editor_api" }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# UI dependencies
gpui = { workspace = true }
ui = { workspace = true }

# Blueprint editor core (reuse existing implementation)
# We'll move the existing blueprint editor code here
pulsar_std = { workspace = true }
# pulsar_engine = { path = "../../crates/pulsar_engine" }

[lib]
crate-type = ["cdylib", "rlib"]
name = "blueprint_editor_plugin"
